apiVersion: v1
kind: Service
metadata:
  name: movie-frontend
spec:
  type: LoadBalancer
  selector:
    app: movie-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP