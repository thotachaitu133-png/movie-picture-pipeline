apiVersion: v1
kind: Service
metadata:
  name: movie-backend
spec:
  type: ClusterIP
  selector:
    app: movie-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP